# You said your API is already running as:
# docker run -p 5001:5000 gm-api
# On macOS, containers can reach the host via host.docker.internal:5001
GM_API_BASE_URL=http://host.docker.internal:5001
GM_API_USERNAME=bh25
GM_API_PASSWORD=gestaltmatcher

# MCP proxy settings
MCP_PROXY_PORT=18080
MCP_PROXY_ALLOW_ORIGIN=*
API_NAME=gestaltmatcher
# API_SPEC_URL can be left unset (defaults to ${GM_API_BASE_URL}/openapi.json)
